[project]
name = "codex-remote-gpu-dev"
version = "0.2.4"
description = "A profile-driven Codex Skill for safe remote GPU development"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pyright]
pythonVersion = "3.11"
